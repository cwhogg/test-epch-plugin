# General memo — placeholder

**Status:** no source exemplar available. The `memos/` folder in source contained only a payer 2-pager (BCBS, reclassified to `payer-pitch-deck`) and a fundraise Series A memo (reclassified to `fundraise-pitch-deck`). Neither is the kind of internal strategy memo this variant is designed for.

**What goes here:** real Marley internal strategy memos, one-pagers, partnership memos, or board-update memos. As Chris generates these, drop them into this folder with the `general-` prefix (`general-<topic>.md`) and update `format.md` to reference them.

**Until then:** the general-memo variant draws structure from `format.md` (Variant A scaffold) and voice from `../../marley-voice/voice.md`. Patient/product narrative docs in `marley-context/` source — particularly the [[../../marley-context/company]] one-pager content — can serve as adjacent reference material for how Marley writes about itself in mid-form prose.

**Likely first additions** (suggested):
- A canonical internal strategy memo on a recent decision (e.g. "Why we're expanding to cardiometabolic", "Why we're going broad-in-network in Florida")
- A one-pager on a single Marley thesis ("Why B2C2B", "Why specialized primary care")
- A partner-pitch memo (e.g. memo to a health system or payer prospect that's not deck-formatted)

When at least one real exemplar exists, retire this placeholder file.
